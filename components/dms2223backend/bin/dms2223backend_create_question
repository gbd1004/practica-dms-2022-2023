#!/usr/bin/env python3

from dms2223common.data import Role
from dms2223backend.data.config import BackendConfiguration
#from dms2223backend.data.db import Schema #TODO:
#from dms2223backend.service import #TODO: crear servicios (ver auth service)

cfg: BackendConfiguration = BackendConfiguration()
cfg.load_from_file(cfg.default_config_file())
#db: Schema = Schema(cfg)
#service1.create_question(argumentos, db, cfg)
